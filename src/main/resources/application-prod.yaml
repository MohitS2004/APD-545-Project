spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blueharbor
    username: blueharbor_app
    password: changeme
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
